#include <stdio.h>
#include <cs50.h>
#include <string.h>
#include <ctype.h>

int main(int argc, string argv[])
{
    if (argc == 2)
    {
        printf("Success!\n");
    }
    if (argc != 2)
    {
        printf("Usage: ./ceasar key\n");
    }
    // if (isalpha(argv[1]))
    // string text = get_string("plaintext: ")
    
}