#include <stdio.h>
#include <cs50.h>
#include <string.h>
#include <strings.h>
#include <ctype.h>
#include <stdlib.h>

// Gets key from user at command line
int main(int argc, string argv[])
{
    if (argc != 2)
    {
        printf("blabla\n");
        return 1;
    }
    if (str)
    else
    {
        for (int i = 0; i < strlen(argv[1]); i++)
        {
            if (isalpha(argv[1][i]) == 0)
            {
                printf("Key only alpha plz.\n");
                return 1;
            }
        }    
    }
    
}